import React from 'react';
import { Text } from "react-native"
import { SafeArea } from '../../../utils/components/safe-area.components';

export const PaymentScreen = () => {
    return (
        <SafeArea>
            <Text>Payments section</Text>
        </SafeArea>
    )
}